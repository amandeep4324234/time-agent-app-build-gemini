# KV Sync Adapter Placeholder (Ticket 4)

This adapter is intentionally empty in v1 according to MASTER-BUILD-SPEC.md §2.2 & Non-Goals §10.1.15.
Contract interfaces and wire structures are defined in `src/lib/types.ts` and `src/lib/ports.ts`.
Implementation will land in Ticket 4.
